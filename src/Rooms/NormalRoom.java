/**Mir Shahiduzzaman November 9*/
package Rooms;

public class NormalRoom extends Room {
    /**
     * Sets up the variables for NormalRoom
     * @param x the row NormalRoom is in
     * @param y the column NormalRoom is in
     */
    public NormalRoom(int x, int y)
    {
        super(x,y);
    }
}
